# Core Dependencies for Louisiana Census Data Agent
# These are the ONLY packages needed to run the application

# Data processing
pandas>=2.2.0

# HTTP requests (Ollama API + Census API)
requests>=2.32.0

# Data validation
pydantic>=2.8.0
pydantic-core>=2.20.0  # Required by pydantic

# Fuzzy string matching (variable resolution)
rapidfuzz>=3.9.0

# Shapefile reading (TIGER/Line tract areas)
pyshp>=2.3.0

# GUI Dependencies (for Streamlit interface)
streamlit>=1.28.0

# Map visualization
folium>=0.15.0
streamlit-folium>=0.15.0
